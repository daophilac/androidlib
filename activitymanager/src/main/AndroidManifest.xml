<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.peanut.activitymanager">

    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <application>
        <receiver android:name=".ActivityFromDeath" />
    </application>
</manifest>
